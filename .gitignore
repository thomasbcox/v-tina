# OS
.DS_Store

# Node / Next.js
node_modules/
.next/
out/
build/
*.tsbuildinfo

# Env — whole family ignored; the committed example is the one exception
.env
.env.*
!.env.example

# Vercel
.vercel/

# Review publish temps (a round killed mid-publish can leave one behind)
reviews/.*.tmp

# Package managers (pnp/yarn)
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Test coverage
/coverage

# Certificates and debug logs
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Next.js generated types (regenerated on build)
next-env.d.ts
