# /dev-audit — v-tina — 2026-08-30

## Detected profile

- **Type:** documentation / configuration repo. No application code of any kind.
- **Ecosystem(s):** Markdown only (`AGENTS.md`). No `package.json`, `pyproject.toml`, `go.mod`,
  `Cargo.toml`, `Gemfile`, `pom.xml`, `composer.json`, `Dockerfile`, `*.tf`, or k8s manifests.
  No shell scripts, so the estate `shellcheck` + `shfmt -i 2 -ci` standard has nothing to bind to yet.
- **Domain:** not security-sensitive — no auth, payments, or PII, because there is no code.
  The repo's only content is reviewer guidance governing this repo's own review loop.
- **Tracked files:** 2 (`AGENTS.md`, `.gitignore`). 2 commits. Public on GitHub.
- **Maturity tier:** **prototype** (see Risk level).

## Tools chosen — and why

| Tool | Selected by (detected marker) | Status |
|---|---|---|
| markdownlint | Markdown row — `AGENTS.md` matches `*.md` | **recommended (not installed)** |
| lychee (link check) | Markdown row — advisory, network-dependent | **recommended (not installed)** |
| gitleaks | Any/cross-cutting — secrets | **ran** — 0 leaks, full history |
| trufflehog | Any/cross-cutting — secrets (alternate to gitleaks) | present; not run — redundant with gitleaks on a 2-file, 2-commit history |
| semgrep | Any/cross-cutting — SAST | **ran** — 0 findings, 2 files scanned |
| osv-scanner | Any/cross-cutting — dependency scan | **ran** — no package sources to scan |
| Architecture review | all repos (non-command entry) | n/a — no architecture exists to review |

All tools were invoked in read-only mode. No target file was modified by any scanner.

## Findings

Ordered most severe first.

1. **No test gate exists (medium).** No `tests/`, no test runner, no configured test command.
   Beyond the general safeguard gap, this has a concrete present-day consequence: `/review`
   step 3 (the gate) has nothing to run, and `/frame` step 9's demonstrate-red obligation has
   no mechanism.
2. **No CI (medium).** No `.github/workflows/`, `.gitlab-ci.yml`, or `.circleci/`. Nothing
   independently verifies a change. For a now-public repo this also means no signal to anyone
   reading it that changes are checked.
3. **No `.claude/workflow.json` (medium, workflow-blocking).** `/review` and `/close` load
   `slug`, `baseBranch`, and `testCommand` from it. Absent, so `/review` stops at step 1.
   `/frame` step 1 is what bootstraps this file; this repo has never been through `/frame`.
4. **No LICENSE (low, elevated by public status).** An unlicensed public repo is
   "all rights reserved" by default while most readers assume public means reusable.
5. **No README (low).** A public repo with no README gives a visitor no way to tell what
   v-tina is.
6. **`main` is not on the remote (low, workflow-relevant).** Only `claude/agents-gherkin-scope`
   was pushed, so GitHub's default branch is the feature branch and no PR can target `main`
   (`/review` step 4).

**Clean:** secret handling. Five pattern detectors across the working tree, a full-history
scan, and gitleaks over all commits returned **0 hits**. No tracked `.env*`. `.gitignore` is
present and covers `.DS_Store`. Dependency pinning is **not applicable** — there are no
dependencies and no manifest to pin.

## Risk level

**Medium.** Driven by the Table B row *"No CI **and** no tests"* (risk medium, maturity ceiling
prototype). The *"Missing README or license"* row also matched (risk low, ceiling developing).
Roll-up takes the most severe risk (**medium**) and the lowest ceiling (**prototype**).

Two rows deliberately judged **not** matched, recorded so the call is reviewable:
- *"Dependencies unpinned (no lockfile / floating ranges)"* — literally there is no lockfile,
  but there are also zero dependencies and no manifest. The row's intent is dependencies that
  exist going unpinned; nothing here is unpinned because nothing is depended on. Revisit the
  moment a manifest lands.
- *"Security-sensitive domain and no/weak tests"* — the domain is not security-sensitive today,
  but that is a property of the repo being empty, not a durable fact.

## Best-practice gaps

- no CI
- no/weak tests
- missing docs/license

## Prioritized next steps

1. **Decide the license.** The repo is already public, so this is the only item with an
   externally-visible clock on it. Adding one later is free; retracting one after others build
   on it is not.
2. **Push `main` and set it as GitHub's default branch.** Cheap, and it unblocks PR-based review.
3. **Establish the workflow config and a test gate together** — `.claude/workflow.json` plus
   whatever `testCommand` should mean in a docs repo (a markdown lint plus a link check is a
   legitimate gate). This is what currently blocks `/review` end to end.
4. **Add CI** running that same gate, so the check is independent of the local machine.
5. **Add a README** stating what v-tina is.
6. **Install markdownlint and lychee** if docs remain the repo's substance. Both are
   install-time privileged on this machine (Homebrew is owned by `thomasadmin`), so they are a
   step for the admin account or a user-local install under `~/.local/bin` — not something this
   session can do.
